#include <stdlib.h>

#include "sylimits.h"
#include "data.h"
#include "error.h"

void
init_rtt(struct stream *st)
{
    fatal("rtt module not available");
}
void
gets_rtt()
{
    fatal("rtt module not available");
}
int
get_rtt(char *symon_buf, int maxlen, struct stream *st)
{
    fatal("rtt module not available");

    /* NOT REACHED */
    return 0;
}
